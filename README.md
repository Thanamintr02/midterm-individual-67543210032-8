# Library Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** [นาย ธนมินทร์ เปลี่ยนพร้อม]
- **Student ID:** [67543210032-8]
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
src/
├── presentation/      # Layer 1: รับส่งข้อมูล HTTP (Routes & Controllers)
├── business/          # Layer 2: กฎทางธุรกิจและการตรวจสอบ (Services & Validators)
├── data/              # Layer 3: การเข้าถึงฐานข้อมูล (Repositories & Connection)
public/                # Frontend (HTML, CSS, JS)
server.js              # Entry point ของระบบ

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
- Tightly Coupled: โค้ดทุกอย่างผูกติดกัน แก้ไขส่วนหนึ่งอาจกระทบส่วนอื่นได้ง่าย

- Difficult to Maintain: ไฟล์ server.js มีขนาดใหญ่เกินไป ทำให้หาจุดผิด (Debug) ได้ยาก

- No Separation of Concerns: Logic การคำนวณ SQL และการตอบกลับ HTTP ปนอยู่ในที่เดียว

- Hard to Test: ไม่สามารถทดสอบเฉพาะส่วน Business Logic ได้โดยไม่รัน Database

### วิธีแก้ไขด้วย Layered Architecture:
- Decoupling: แยกหน้าที่ออกจากกันชัดเจนผ่าน 3 ชั้น (Presentation, Business, Data)

- Modularization: แบ่งโค้ดเป็นไฟล์ย่อยๆ ตามหน้าที่ ทำให้จัดการง่าย

- Validation Injection: แยกส่วนตรวจสอบข้อมูล (Validator) ออกจาก Logic หลัก

- Standardized Error Handling: ใช้ Middleware กลางจัดการ Error ทั้งหมดในที่เดียว

### ประโยชน์ที่ได้รับ:
- Maintainability: โค้ดอ่านง่ายขึ้นและดูแลรักษาได้ในระยะยาว

- Scalability: สามารถขยายระบบได้ง่าย เช่น เปลี่ยน Database จาก SQLite เป็น MongoDB ได้โดยแก้แค่ Data Layer

- Reusability: Business Logic ใน Service สามารถนำไปใช้ซ้ำใน Controller อื่นได้

- Testability: สามารถเขียน Unit Test ทดสอบเฉพาะส่วน Service ได้ง่ายขึ้น

## 🚀 How to Run

\`\`\`bash
# 1. Clone repository
git clone (https://github.com/Thanamintr02/midterm-individual-67543210032-8)

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000
\`\`\`

## 📝 API Endpoints
GET /api/books - ดึงรายชื่อหนังสือและสถิติทั้งหมด

GET /api/books/:id - ดึงรายละเอียดหนังสือตาม ID

POST /api/books - เพิ่มหนังสือใหม่

PUT /api/books/:id - แก้ไขข้อมูลหนังสือ

PATCH /api/books/:id/borrow - ยืมหนังสือ

PATCH /api/books/:id/return - คืนหนังสือ

DELETE /api/books/:id - ลบหนังสือ